# Payc2 充值平台集成总结

## 概述

本文档总结了 payc2 充值平台的完整集成过程，包括核心功能实现、配置说明、测试验证和使用指南。

## 核心文件

### 1. 签名处理器
- **文件**: `internal/signature/payc2.go`
- **功能**: 实现 MD5 签名算法、请求参数构建、回调签名验证
- **特性**: 支持参数排序、运营商识别、签名生成和验证

### 2. 平台处理器
- **文件**: `internal/service/recharge/payc2.go`
- **功能**: 实现充值订单提交、状态查询、回调数据解析
- **特性**: HTTP 请求处理、响应解析、订单状态管理

### 3. 平台注册
- **文件**: `internal/service/recharge/manager.go`
- **功能**: 注册 payc2 平台类型，支持动态创建平台实例
- **修改**: 添加 `payc2` 平台类型和创建逻辑

### 4. 回调路由
- **文件**: `internal/router/callback.go`
- **功能**: 注册 payc2 回调路由
- **路径**: `/callback/payc2/:userid`

### 5. 回调控制器
- **文件**: `internal/controller/callback.go`
- **功能**: 处理 payc2 平台异步回调
- **特性**: 参数解析、签名验证、状态更新

## 配置文件

### 1. 数据库配置脚本
- **文件**: `scripts/setup_payc2_platform.sql`
- **功能**: 初始化 payc2 平台的数据库配置
- **内容**: 平台信息、账号配置、API 参数

### 2. 集成文档
- **文件**: `docs/payc2_platform_setup.md`
- **功能**: 详细的集成指南和使用说明
- **内容**: 配置步骤、API 说明、回调处理、故障排查

## 测试文件

### 1. 单元测试
- **文件**: `test/payc2_test.go`
- **功能**: 全面的功能测试和性能测试
- **覆盖**: 签名生成、运营商识别、平台创建、参数构建、回调解析

## 技术特性

### 1. MD5 签名算法
- 参数按字典序排序
- 拼接商户秘钥
- MD5 加密生成签名

### 2. 运营商识别
- 自动根据手机号前缀识别运营商
- 支持移动(YD)、联通(LT)、电信(DX)

### 3. API 接口
- HTTP POST 请求
- application/x-www-form-urlencoded 格式
- 完整的错误处理和日志记录

### 4. 异步回调处理
- 支持订单状态异步通知
- 自动签名验证
- 多种订单状态处理（成功、失败、部分充值、超充）
- 回调重试机制

### 5. 错误处理优化
- HTTP状态码检查（处理520等错误）
- 非JSON响应格式处理
- 详细的错误信息记录
- 网络异常和平台错误区分

## 配置参数

### 平台配置
```sql
-- 平台基本信息
platform_code: payc2
platform_name: Payc2充值平台
api_url: https://api.payc2.com/recharge

-- 商户配置
merch: 商户编号
app_secret: 商户密钥
```

### API 参数
- `merch`: 商户编号
- `orderNo`: 订单号
- `amount`: 充值金额（分）
- `phone`: 手机号码
- `telco`: 运营商（YD/LT/DX）
- `notifyUrl`: 回调地址
- `timeoutSecond`: 超时时间
- `sign`: MD5签名

## 回调处理

### 回调参数
- `merch`: 商户编号
- `uid`: 系统订单ID
- `orderNo`: 商户订单号
- `amount`: 订单金额
- `amountPaid`: 已充金额
- `stateAmount`: 金额状态（0零充值，1已全充，3部分充，4已超充）
- `stateOver`: 结束状态（0未结束，1已结束）
- `sign`: 签名

### 状态映射
- `stateAmount=0`: 零充值 → 订单失败
- `stateAmount=1`: 已全充 → 订单成功
- `stateAmount=3`: 部分充 → 部分成功
- `stateAmount=4`: 已超充 → 订单成功

## 使用流程

### 1. 数据库配置
```bash
mysql -u root -p < scripts/setup_payc2_platform.sql
```

### 2. 修改配置参数
- 更新商户编号和密钥
- 设置正确的 API 地址
- 配置回调地址

### 3. 提交充值订单
```go
manager := recharge.NewManager(db)
manager.LoadPlatforms()
result := manager.SubmitOrder(order)
```

### 4. 处理回调通知
- 系统自动处理回调
- 验证签名
- 更新订单状态

## 测试验证

### 运行测试
```bash
go test ./test/payc2_test.go -v
```

### 测试覆盖
- ✅ 签名生成测试
- ✅ 运营商识别测试
- ✅ 平台创建测试
- ✅ 请求参数构建测试
- ✅ 回调数据解析测试
- ✅ 性能基准测试

## 安全考虑

### 1. 签名验证
- 所有请求和回调都进行 MD5 签名验证
- 防止数据篡改和重放攻击

### 2. 参数校验
- 严格的参数类型和格式验证
- 防止注入攻击

### 3. 错误处理
- 完整的错误日志记录
- 敏感信息脱敏处理

## 监控和日志

### 关键日志
- 订单提交日志
- 回调处理日志
- 签名验证日志
- 错误异常日志

### 监控指标
- 订单成功率
- 回调处理延迟
- 签名验证失败率
- API 响应时间

## 故障排查

### 常见问题
1. **签名验证失败**
   - 检查商户密钥配置
   - 验证参数排序逻辑
   - 确认 MD5 算法实现

2. **回调处理失败**
   - 检查回调地址配置
   - 验证网络连通性
   - 查看回调日志

3. **订单状态异常**
   - 检查状态映射逻辑
   - 验证回调参数解析
   - 查看订单更新日志

## 版本信息

- **集成版本**: v1.0.0
- **平台版本**: v1.240813
- **创建时间**: 2024年
- **最后更新**: 2024年

## 后续优化

### 功能增强
1. 支持订单状态主动查询
2. 实现账户余额查询
3. 添加批量订单处理
4. 支持更多运营商类型

### 性能优化
1. 签名算法性能优化
2. HTTP 请求连接池
3. 回调处理异步化
4. 缓存机制优化

### 监控完善
1. 实时监控面板
2. 告警机制
3. 性能分析
4. 业务指标统计

---

**注意**: 在生产环境使用前，请确保完成所有测试验证，并根据实际业务需求调整配置参数。