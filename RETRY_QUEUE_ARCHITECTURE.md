# 重试队列架构说明

## 概述

本文档描述了重试任务从直接调用改为消息队列模式的架构重构。新架构实现了 server 服务和 recharge 服务的职责分离，提高了系统的可扩展性和可靠性。

## 架构变更

### 原架构问题

1. **职责混乱**：server 服务和 recharge 服务都具备重试功能
2. **紧耦合**：回调处理直接调用重试服务
3. **资源竞争**：两个服务同时处理重试任务

### 新架构设计

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   外部回调      │    │   消息队列      │    │   重试服务      │
│   (Server)      │───▶│   (Redis)       │───▶│   (Recharge)    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 核心组件

### 1. 重试任务消息 (RetryTaskMessage)

```go
type RetryTaskMessage struct {
    OrderID   int64     `json:"order_id"`   // 订单ID
    RetryType int       `json:"retry_type"` // 重试类型
    Reason    string    `json:"reason"`     // 重试原因
    CreatedAt time.Time `json:"created_at"` // 创建时间
}
```

### 2. 队列配置

```yaml
retry_task:
  queue_name: "retry_queue"  # 重试队列名称
  consumer_count: 2          # 消费者数量
  poll_interval: 1           # 队列轮询间隔（秒）
```

### 3. 服务职责分工

#### Server 服务
- 接收外部回调
- 检查是否有可用通道
- 推送重试任务到消息队列
- **不再直接执行重试逻辑**

#### Recharge 服务
- 从消息队列消费重试任务
- 执行具体的重试逻辑
- 保持定时扫描作为备用机制

## 工作流程

### 1. 回调处理流程

```
外部回调 → 检查可用通道 → 推送到队列 → 返回成功
```

### 2. 重试处理流程

```
队列消费 → 解析任务 → 获取订单 → 执行重试 → 记录结果
```

## 优势

### 1. 职责分离
- Server 专注于 API 处理
- Recharge 专注于业务逻辑

### 2. 解耦合
- 异步处理，提高响应速度
- 队列缓冲，处理突发流量

### 3. 可扩展性
- 支持多消费者并发处理
- 可独立扩展各服务

### 4. 可靠性
- 消息持久化，防止丢失
- 备用机制，双重保障

## 配置说明

### 队列配置项

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| queue_name | 队列名称 | retry_queue |
| consumer_count | 消费者数量 | 1 |
| poll_interval | 轮询间隔(秒) | 1 |

### 重试配置项

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| interval | 定时扫描间隔(秒) | 30 |
| max_retries | 最大重试次数 | 3 |
| retry_delay | 重试延迟(秒) | 60 |

## 部署建议

### 1. 生产环境

```bash
# 启动 server 服务（处理 API 请求）
make run-server

# 启动 recharge 服务（处理重试任务）
make run-recharge
```

### 2. 开发环境

```bash
# 测试重试队列功能
go run test_retry_queue.go
```

## 监控指标

### 队列监控
- 队列长度
- 消费速率
- 错误率

### 重试监控
- 重试成功率
- 平均处理时间
- 失败原因分布

## 故障处理

### 1. 队列故障
- 定时扫描机制作为备用
- 重试记录持久化到数据库

### 2. 消费者故障
- 多消费者并发处理
- 自动重启机制

### 3. Redis 故障
- 降级到定时扫描模式
- 告警通知运维人员

## 测试验证

### 1. 功能测试
```bash
# 运行队列测试
go run test_retry_queue.go
```

### 2. 压力测试
- 模拟大量回调请求
- 验证队列处理能力
- 监控系统资源使用

### 3. 故障测试
- Redis 服务中断
- 消费者进程异常
- 网络延迟测试

## 注意事项

1. **Redis 配置**：确保 Redis 服务稳定运行
2. **消费者数量**：根据业务量调整消费者数量
3. **监控告警**：设置队列长度和错误率告警
4. **数据备份**：重要重试记录需要持久化
5. **版本兼容**：确保新旧版本平滑过渡